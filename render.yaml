# Render.com blueprint - deploy at https://dashboard.render.com (Python, no Docker)
services:
  - type: web
    name: student-predict
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT predict_app:app
